stages:
  update-sql:
    actions:
      - type: liquibase
        command: UpdateSQL
        cmdArgs: {changeLogFile: changelog/changelog.xml}
        properties: liquibase.properties
  
  update:
    actions:
      - type: liquibase
        command: Update
        cmdArgs: {changeLogFile: changelog/changelog.xml}
        properties: liquibase.properties

endStage:
    actions:
      - type: liquibase
        command: History